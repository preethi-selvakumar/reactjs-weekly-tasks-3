import React from 'react';

const Admin = () => {
    return (
        <div className="container">
            <h2>Admin Panel</h2>
            <p>Only accessible if you are logged in (token exists).</p>
        </div>
    );
};

export default Admin;
